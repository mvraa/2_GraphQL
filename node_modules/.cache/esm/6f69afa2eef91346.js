let buildSchema;_eef‍.x([["schema",()=>schema],["rootValue",()=>rootValue]]);_eef‍.w("graphql",[["buildSchema",["buildSchema"],function(v){buildSchema=v}]]);


       const schema = buildSchema(`
  type Query {
    currentTime: String!
  }
`);

// resolver functions
       const rootValue = {
  currentTime: () => {
    const isoString = new Date().toISOString();
    return isoString.slice(11, 19);
  },
};